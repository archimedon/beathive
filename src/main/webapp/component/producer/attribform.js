$(document).ready(function(){
	var loopId = ${param.id};
	// "done" button clicked
	$('#done').click(function(){
		var field = this;
		// get parentNode
		var par = $(field).parents("td.editing")[0];
		// get currently selected instrument id 
		});
		
		
		return false;
	});
});
